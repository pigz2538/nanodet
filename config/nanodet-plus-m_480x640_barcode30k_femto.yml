# NanoDet-Plus Barcode-30k Femto
# 比 Pico 再小一级：MobileNetV2 0.25x + FPN/Head 16 ch
# 参数量 ~0.21M，预估 ONNX ~0.7 MB
# 通过调大 batch、增强 aug、提高 lr、减少 weight decay 来尽量维持精度
# Input tensor shape: 1 x 1 x 480 x 640 (NCHW)

save_dir: workspace/nanodet-plus-m_barcode30k_femto
model:
  weight_averager:
    name: ExpMovingAverager
    decay: 0.9998
  arch:
    name: NanoDetPlus
    detach_epoch: 10
    backbone:
      name: MobileNetV2
      width_mult: 0.25
      out_stages: [2, 4, 6]
      last_channel: 80         # 320 * 0.25
      in_channels: 1
      activation: LeakyReLU
      pretrain: False
    fpn:
      name: GhostPAN
      in_channels: [8, 24, 80]   # MobileNetV2 0.25x 输出
      out_channels: 16
      kernel_size: 3
      num_extra_level: 0
      use_depthwise: True
      activation: LeakyReLU
      upsample_cfg:
        scale_factor: 2
        mode: nearest
    head:
      name: NanoDetPlusHead
      num_classes: 2
      input_channel: 16
      feat_channels: 16
      stacked_convs: 1
      kernel_size: 3
      strides: [8, 16, 32]
      activation: LeakyReLU
      reg_max: 7
      norm_cfg:
        type: BN
      loss:
        loss_qfl:
          name: QualityFocalLoss
          use_sigmoid: True
          beta: 2.0
          loss_weight: 1.0
        loss_dfl:
          name: DistributionFocalLoss
          loss_weight: 0.25
        loss_bbox:
          name: GIoULoss
          loss_weight: 2.0
    aux_head:
      name: SimpleConvHead
      num_classes: 2
      input_channel: 32          # 16 * 2 (dual FPN concat)
      feat_channels: 32          # 32 可被 GN group=32 整除
      stacked_convs: 2
      strides: [8, 16, 32]
      activation: LeakyReLU
      reg_max: 7

data:
  train:
    name: CocoDataset
    img_path: dataset/barcode30k_final/train/images
    ann_path: dataset/barcode30k_final/train/train.json
    input_size: [640, 480]
    keep_ratio: True
    grayscale: True
    pipeline:
      perspective: 0.0
      scale: [0.5, 1.5]
      stretch: [[0.7, 1.3], [0.7, 1.3]]
      rotation: 10
      shear: 2
      translate: 0.1
      flip: 0.5
      brightness: 0.15
      contrast: [0.7, 1.3]
      normalize: [[114.4859], [66.9615]]
  val:
    name: CocoDataset
    img_path: dataset/barcode30k_final/val/images
    ann_path: dataset/barcode30k_final/val/val.json
    input_size: [640, 480]
    keep_ratio: True
    grayscale: True
    pipeline:
      normalize: [[114.4859], [66.9615]]

  # test:
  #   name: CocoDataset
  #   img_path: dataset/barcode30k_final/test/images
  #   ann_path: dataset/barcode30k_final/test/test.json
  #   input_size: [640, 480]
  #   keep_ratio: True
  #   grayscale: True
  #   pipeline:
  #     normalize: [[114.4859], [66.9615]]

device:
  gpu_ids: [0]
  workers_per_gpu: 16
  batchsize_per_gpu: 192       # 极小模型用更大 batch
  precision: 32

schedule:
  optimizer:
    name: AdamW
    lr: 0.002                  # 更小模型需要更高 lr
    weight_decay: 0.01
  warmup:
    name: linear
    steps: 5000
    ratio: 0.0001
  total_epochs: 1000
  lr_schedule:
    name: CosineAnnealingLR
    T_max: 1000
    eta_min: 0.00005
  val_intervals: 1

grad_clip: 35
evaluator:
  name: CocoDetectionEvaluator
  save_key: mAP

log:
  interval: 10

vis:
  num_images: 5
  score_thresh: 0.5

class_names: ['barcode', 'qr_code']
